experiment:
  name: baseline_ewc_cl
  baseline: ewc
  dataset: PROTEINS
  n_tasks: 5
  seed_split: 0
  seed_model: 42

training:
  epochs_per_task: 30
  batch_size: 32
  optimizer: adamw
  lr: 1.0e-2
  weight_decay: 1.0e-4

model:
  hidden_dim: 64
  num_layers: 2

pjepa:
  ewc_lambda: 100.0
